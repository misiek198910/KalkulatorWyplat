package com.example.kalkulatorwyplat.ui.theme

import androidx.compose.ui.graphics.Color

// --- NOWA PALETA FINTECH DARK GREEN ---

// Główne tło aplikacji (bardzo ciemna zieleń, prawie czarna)
val BackgroundDarkGreen = Color(0xFF091A14)

// Kolor "powierzchni" (np. kart, paneli) - nieco jaśniejszy od tła
val SurfaceDarkGreen = Color(0xFF132A22)

// Kolor akcentujący (żywa limonka) - do przycisków, przełączników, najważniejszych wyników
val AccentLime = Color(0xFF86D957)

// Kolor dodatkowy dla elementów interaktywnych (spokojniejsza zieleń)
val PrimaryTeal = Color(0xFF26A69A)

// Kolory tekstu
val TextWhite = Color(0xFFF0F2F0)
val TextGray = Color(0xFFB0B8B4)

// Kolor błędu (np. w TextField)
val ErrorRed = Color(0xFFCF6679)